{
  "name": "lauren-chess",
  "version": "1.0.0",
  "description": "A multiplayer chess game with a Flask backend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && cd Scripts && activate.bat && cd .. && python app.py"
  },
  "author": "Lauren Hetherington",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.4.3"
  }
}
